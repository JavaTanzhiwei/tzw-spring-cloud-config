server:
  port: 8004
spring:
  application:
    name: tzwGateWay
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
      routes:
        # 系统管理
        - id: tzwModulesSystem
          uri: lb://tzwSystem
          predicates:
            - Path=/sys/**
          filters:
            - StripPrefix=1


