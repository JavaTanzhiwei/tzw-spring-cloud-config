server:
  port: 8004

spring:
  application:
    name: tzwGateWay
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
      loadbalancer:
        retry:
          enabled: true
      routes:
        # 路由规则-系统管理
        - id: tzwSystem
          uri: lb://tzwSystem
          predicates: # 断言（判断条件）
            - Path=/tzwSystem/sys/** # 匹配对应的URL请求，将匹配到的请求追加到目标URL后
